function ninjaWait(milliseconds) {
    console.log("Initiating stealth mode... 🥷");
    setTimeout(() => {
        console.log("Mission accomplished. Code is now invisible!");
    }, milliseconds);
}

// Use it wisely, or not at all!
ninjaWait(3000);